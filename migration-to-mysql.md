# مستندات مهاجرت از Convex به هاست اشتراکی (MySQL)

این مستند مراحل لازم برای مهاجرت پروژه از زیرساخت Serverless و بلادرنگ Convex به یک معماری سنتی مبتنی بر هاست اشتراکی (با دیتابیس MySQL) را توضیح می‌دهد.

## نیازمندی‌های معماری جدید

وقتی شما کدی را روی هاست اشتراکی مستقر می‌کنید، دیتابیس کانوکس دیگر قابل استفاده نیست. شما به یک **بک‌اند اختصاصی** نیاز دارید که روی سرور شما اجرا شود و درخواست‌های فرانت‌اند (React) را به دیتابیس MySQL متصل کند.

معماری پیشنهادی برای هاست‌های اشتراکی:
1. **فرانت‌اند:** React (یا همان Vite/Next.js) که فایل‌های بیلد شده (HTML/CSS/JS) روی هاست قرار می‌گیرند.
2. **بک‌اند (API):** یک فریم‌ورک بک‌اند مثل **Laravel (PHP)** یا **Express (Node.js)**. با توجه به محدودیت‌های هاست‌های اشتراکی ایران، لاراول (PHP) بهترین و پایدارترین گزینه است.
3. **دیتابیس:** MySQL.

---

## مراحلی که باید انجام شوند

### ۱. حذف کانوکس و هوک‌های بلادرنگ
تمام هوک‌های مربوط به کانوکس باید از کل پروژه حذف شوند. این شامل فایل‌ها و پوشه‌های زیر است:
- پوشه `src/convex` به طور کامل حذف می‌شود.
- تمام `useQuery` و `useMutation` ها از `@/convex/react` باید با سیستم جدید جایگزین شوند.
- لاجیک‌های ورود و ثبت‌نام که با `@convex-dev/auth` کار می‌کردند باید حذف شوند.

### ۲. پیاده‌سازی سرویس‌های API (Axios / React Query)
به جای هوک‌های کانوکس، باید از **React Query (TanStack Query)** و **Axios** استفاده کنید تا درخواست‌های HTTP/REST به بک‌اند ارسال شوند.
به عنوان مثال، در فایل `useAuth`:
```typescript
// قبل از مهاجرت:
const user = useQuery(api.users.currentUser);

// بعد از مهاجرت:
const { data: user, isLoading } = useQuery({
  queryKey: ['currentUser'],
  queryFn: () => axios.get('/api/user').then(res => res.data)
});
```

### ۳. توسعه بک‌اند (مثال: لاراول)
شما باید تمام جداول دیتابیسی که در `convex/schema.ts` تعریف شده‌اند (مانند کاربران، محصولات، سفارشات، تیکت‌ها و تنظیمات) را به صورت **مایگریشن (Migration)** در لاراول ایجاد کنید.
سپس برای هر کدام از متدهای کانوکس، یک **Controller** در لاراول بنویسید که داده‌ها را از دیتابیس MySQL بخواند و به صورت JSON برگرداند.

### ۴. مدیریت وضعیت‌ها و تنظیمات سراسری
در نسخه کانوکس، تنظیمات سیستم (مثل تم و غیره) به صورت بلادرنگ از دیتابیس خوانده می‌شدند. در نسخه MySQL باید این تنظیمات هنگام لود اولیه اپلیکیشن از طریق یک API (مثلا `/api/settings`) دریافت شده و در `Zustand` یا `Redux` یا `Context` ذخیره شوند.

---

## نظر و پیشنهاد درباره Laravel 12 + React Starter Kit

پیشنهاد شما برای استفاده از **Laravel 12 به همراه React (مثلاً با Inertia.js یا Laravel Breeze/Jetstream)** یک **ایده فوق‌العاده عالی و کاملاً منطقی** برای بازار ایران و هاست‌های اشتراکی است.

### چرا لاراول برای این پروژه بهتر است؟
1. **پایداری روی هاست اشتراکی (cPanel/DirectAdmin):** لاراول با زبان PHP نوشته شده است که به صورت بومی و ۱۰۰٪ بدون مشکل روی ارزان‌ترین هاست‌های اشتراکی اجرا می‌شود. در حالی که اجرای Node.js روی هاست‌های اشتراکی معمولاً با قطعی، محدودیت رم و مشکلات پروسس (Process) همراه است.
2. **ارتباط عالی با MySQL:** لاراول دارای قدرتمندترین سیستم ORM (Eloquent) است که مدیریت جداول، روابط (Relations) محصولات، سفارشات و کاربران را بسیار ساده و حرفه‌ای می‌کند.
3. **Inertia.js:** در استارتر کیت‌های جدید لاراول، ابزاری به نام Inertia وجود دارد که به شما اجازه می‌دهد فرانت‌اند React را مستقیماً به بک‌اند لاراول متصل کنید، **بدون اینکه نیاز باشد API دستی بنویسید**. این کار سرعت توسعه را به شدت بالا می‌برد و پرش‌های صفحه (Loading) را از بین می‌برد.
4. **اکوسیستم پرداخت:** اتصال لاراول به درگاه‌های پرداخت ایرانی (زرین‌پال، ملت، سامان و...) بسیار ساده‌تر است و پکیج‌های آماده فراوانی برای آن وجود دارد.
5. **سئو (SEO):** اگر فروشگاه را با Laravel + React + SSR پیاده کنید، سئوی سایت شما برای گوگل بسیار بهینه‌تر از یک SPA ساده خواهد بود.

**نتیجه‌گیری:** اگر قصد دارید محصول نهایی را روی هاست اشتراکی نصب کنید و سورس آن را به مشتریان خود بفروشید یا ارائه دهید، معماری فعلی (BaaS/Convex) مناسب نیست. انتقال این UI به **Laravel 12 + React (Inertia)** یک تصمیم استراتژیک، ارزان برای هاستینگ، و بسیار پایدار خواهد بود.
